from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Multimodal music genre classification which extracts features from audio and lyrics to classify music genres across a diverse array of classes.',
    author='Shameek Hargrave',
    license='',
)
